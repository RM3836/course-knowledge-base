# 04-代码模板

本文件汇总AI机器学习实验中常用的Python代码模板，可直接复制使用。

## 一、环境设置模板

```python
# 导入常用库
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
import warnings
warnings.filterwarnings('ignore')

# 设置中文显示（解决中文乱码）
plt.rcParams['font.sans-serif'] = ['SimHei']
plt.rcParams['axes.unicode_minus'] = False

# 设置随机种子（确保结果可复现）
RANDOM_STATE = 42
```

## 二、数据加载模板

### 加载sklearn内置数据集

```python
from sklearn.datasets import load_iris, load_boston

# 鸢尾花数据集
iris = load_iris()
df = pd.DataFrame(data=iris.data, columns=iris.feature_names)
df['target'] = iris.target
df['target_name'] = df['target'].map({0: '山鸢尾', 1: '杂色鸢尾', 2: '维吉尼亚鸢尾'})
```

### 加载CSV文件

```python
# 从本地加载
df = pd.read_csv('data.csv')

# 从URL加载
data_url = "http://lib.stat.cmu.edu/datasets/boston"
df = pd.read_csv(data_url, sep="\s+", skiprows=22, header=None)
```

### 数据探索模板

```python
print(f"数据集形状: {df.shape}")
print(f"数据类型:\n{df.dtypes}")
print(f"缺失值:\n{df.isnull().sum()}")
print(f"统计描述:\n{df.describe()}")
print(f"前5行:\n{df.head()}")
```

## 三、数据可视化模板

### 直方图（带KDE曲线）

```python
plt.figure(figsize=(10, 6))
n, bins, patches = plt.hist(df['column'], bins=10, edgecolor='black', alpha=0.7, density=True)
from scipy.stats import gaussian_kde
kde = gaussian_kde(df['column'])
x_kde = np.linspace(df['column'].min(), df['column'].max(), 200)
plt.plot(x_kde, kde(x_kde), color='red', linewidth=2, label='KDE曲线')
plt.xlabel('X轴标签')
plt.ylabel('密度')
plt.title('标题')
plt.legend()
plt.grid(alpha=0.3)
plt.show()
```

### 散点图矩阵

```python
sns.pairplot(df, hue='target_name', vars=feature_names, markers=['o', 's', '^'])
plt.title('特征散点图矩阵', y=1.02)
plt.show()
```

### 箱线图

```python
plt.figure(figsize=(12, 8))
for i, feature in enumerate(feature_names):
    plt.subplot(2, 2, i+1)
    sns.boxplot(x='target_name', y=feature, data=df, palette='Set2')
    plt.title(f'{feature}的箱线图')
plt.tight_layout()
plt.show()
```

### 相关性热力图

```python
corr_matrix = df[feature_names].corr()
plt.figure(figsize=(10, 8))
sns.heatmap(corr_matrix, annot=True, cmap='coolwarm', fmt='.2f', linewidths=0.5)
plt.title('特征相关性热力图')
plt.tight_layout()
plt.show()
```

### 柱状图对比

```python
plt.figure(figsize=(10, 6))
bars = plt.bar(names, values, color=['#1f77b4', '#ff7f0e', '#2ca02c'], width=0.6)
for bar, val in zip(bars, values):
    plt.text(bar.get_x() + bar.get_width()/2, bar.get_height() + 0.01,
             f'{val:.4f}', ha='center', va='bottom', fontsize=12)
plt.title('对比标题')
plt.ylabel('准确率')
plt.ylim(0.8, 1.05)
plt.grid(axis='y', alpha=0.3)
plt.show()
```

### 真实值vs预测值散点图

```python
plt.figure(figsize=(10, 6))
plt.scatter(y_test, y_pred, alpha=0.6, color='blue', s=30)
plt.plot([y_test.min(), y_test.max()], [y_test.min(), y_test.max()],
         'r--', linewidth=2, label='完美预测线')
plt.xlabel('真实值')
plt.ylabel('预测值')
plt.title('真实值 vs 预测值')
plt.legend()
plt.grid(alpha=0.3)
plt.show()
```

## 四、数据预处理模板

### 数据划分

```python
from sklearn.model_selection import train_test_split

X = df[feature_names]
y = df['target']

X_train, X_test, y_train, y_test = train_test_split(
    X, y, test_size=0.2, random_state=RANDOM_STATE, stratify=y
)
```

### 特征标准化

```python
from sklearn.preprocessing import StandardScaler

scaler = StandardScaler()
X_train_scaled = scaler.fit_transform(X_train)  # 仅用训练集拟合
X_test_scaled = scaler.transform(X_test)        # 测试集用同样的scaler转换
```

## 五、分类模型模板

### KNN分类

```python
from sklearn.neighbors import KNeighborsClassifier
from sklearn.metrics import accuracy_score, classification_report

knn = KNeighborsClassifier(n_neighbors=5)
knn.fit(X_train_scaled, y_train)
y_pred = knn.predict(X_test_scaled)
print(f"准确率: {accuracy_score(y_test, y_pred):.4f}")
print(classification_report(y_test, y_pred, target_names=target_names))
```

### 决策树分类

```python
from sklearn.tree import DecisionTreeClassifier, plot_tree

dt = DecisionTreeClassifier(random_state=RANDOM_STATE)
dt.fit(X_train_scaled, y_train)
y_pred = dt.predict(X_test_scaled)

# 决策树可视化
plt.figure(figsize=(15, 10))
plot_tree(dt, feature_names=feature_names, class_names=target_names,
          filled=True, rounded=True, fontsize=10)
plt.show()
```

### SVM分类

```python
from sklearn.svm import SVC

svm = SVC(kernel='rbf', random_state=RANDOM_STATE)
svm.fit(X_train_scaled, y_train)
y_pred = svm.predict(X_test_scaled)
print(f"准确率: {accuracy_score(y_test, y_pred):.4f}")
```

## 六、回归模型模板

### 线性回归

```python
from sklearn.linear_model import LinearRegression
from sklearn.metrics import mean_squared_error, r2_score

lr = LinearRegression()
lr.fit(X_train_scaled, y_train)
y_pred = lr.predict(X_test_scaled)

mse = mean_squared_error(y_test, y_pred)
rmse = np.sqrt(mse)
r2 = r2_score(y_test, y_pred)
print(f"MSE: {mse:.3f}, RMSE: {rmse:.3f}, R²: {r2:.3f}")
```

### Ridge回归（带参数调优）

```python
from sklearn.linear_model import Ridge
from sklearn.model_selection import GridSearchCV

ridge = Ridge(random_state=RANDOM_STATE)
grid_search = GridSearchCV(
    estimator=ridge,
    param_grid={'alpha': [0.01, 0.1, 1, 10, 100]},
    cv=5,
    scoring='r2'
)
grid_search.fit(X_train_scaled, y_train)
print(f"最佳alpha: {grid_search.best_params_['alpha']}")
print(f"最佳R²: {grid_search.best_score_:.3f}")
```

## 七、聚类模型模板

### K-Means聚类

```python
from sklearn.cluster import KMeans
from sklearn.metrics import silhouette_score

# 肘部法则
wcss = []
for k in range(1, 11):
    kmeans = KMeans(n_clusters=k, init='k-means++', random_state=RANDOM_STATE)
    kmeans.fit(X_scaled)
    wcss.append(kmeans.inertia_)

plt.plot(range(1, 11), wcss, marker='o')
plt.xlabel('K')
plt.ylabel('WCSS')
plt.title('肘部法则')
plt.show()

# 训练模型
kmeans = KMeans(n_clusters=5, init='k-means++', random_state=RANDOM_STATE)
y_pred = kmeans.fit_predict(X_scaled)

# 评估
sil_score = silhouette_score(X_scaled, y_pred)
print(f"轮廓系数: {sil_score:.3f}")
```

## 八、特征重要性模板

```python
# 线性回归特征重要性
feature_importance = pd.DataFrame({
    '特征': X.columns,
    '重要性': np.abs(lr.coef_)
}).sort_values(by='重要性', ascending=False)

plt.figure(figsize=(10, 6))
plt.barh(feature_importance['特征'], feature_importance['重要性'], color='skyblue')
plt.title('特征重要性排序')
plt.xlabel('重要性(系数绝对值)')
plt.grid(alpha=0.3)
plt.show()
```

## 九、常用评估指标速查

```python
from sklearn.metrics import (
    accuracy_score, precision_score, recall_score, f1_score,  # 分类
    classification_report, confusion_matrix,                   # 分类报告
    mean_squared_error, r2_score, mean_absolute_error          # 回归
)

# 分类指标
print(f"准确率: {accuracy_score(y_test, y_pred):.4f}")
print(classification_report(y_test, y_pred))

# 回归指标
print(f"MSE: {mean_squared_error(y_test, y_pred):.3f}")
print(f"RMSE: {np.sqrt(mean_squared_error(y_test, y_pred)):.3f}")
print(f"R²: {r2_score(y_test, y_pred):.3f}")
print(f"MAE: {mean_absolute_error(y_test, y_pred):.3f}")
```
